(ns team-tree.navigation.styles
  (:require [stylefy.core :as stylefy]
            [themes.typography :as typography]
            [themes.palette :refer [color shade]]
            [team-tree.constants :refer [css-vendors]]))

(def zoom
  {:color "red"
   :grid-area "tools"
   :padding "0 1.0em"})
